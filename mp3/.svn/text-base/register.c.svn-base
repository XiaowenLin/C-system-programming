#include "register.h"
#include <stdio.h>
#include <stdlib.h>

void register_pass(int curr_id, int old_id)
{
  printf("TASK ID: %d PASS(%d)\n", curr_id, old_id);	
}
	
void register_merge(int curr_id, int old_id_a, int old_id_b, int old_size_a, int old_size_b)
{
  printf("TASK ID: %d MERGE(%d,%d): merge %d elements and %d elements\n", curr_id, old_id_a, old_id_b, old_size_a, old_size_b);	
}

void register_qsort(int curr_id, int old_id)
{
  printf("TASK ID: %d QSORT %d elements\n", curr_id, old_id);
}
